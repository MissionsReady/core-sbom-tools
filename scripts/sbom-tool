#!/usr/bin/env python3
"""Repository-local entrypoint for MissionsReady SBOM tooling."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))

from missionsready_sbom.cli import main

raise SystemExit(main())
